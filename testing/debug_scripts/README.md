# Parity debug scripts

One-off investigation scripts written during parity fixes. Not picked up by pytest.

Keep for reproducing the original investigation if a related regression shows up. If you're adding a new family or investigating a new parity bug, write a similar script here rather than polluting `testing/` with non-test files.
